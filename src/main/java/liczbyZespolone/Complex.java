package liczbyZespolone;

public class Complex {
    /*
    Write a class named Complex. It must have two double
    fields real, imaginary and a constructor to initialize
    the fields. The order of parameters in the constructor
    must be the same as presented above.
    */
    double real, image;

    public Complex(double real, double image) {
        this.real = real;
        this.image = image;
    }
}
